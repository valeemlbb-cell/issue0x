{
  "name": "issue0x-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The issue0x agentic trader runtime — Opus 4.8 brain, risk gate, sealed-and-scored positions. Paper by default; wire market data + execution + an Anthropic key to go live.",
  "scripts": {
    "start": "tsx src/index.ts",
    "loop": "tsx src/index.ts --once",
    "backtest": "tsx src/index.ts --backtest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "tsx": "^4.23.1",
    "typescript": "~6.0.2"
  }
}
